# 運行管理システム 引き継ぎ文書

---

## システム概要
- Google Apps Script (GAS) V8 + SpreadsheetApp + PropertiesService
- ローカルファイル: c:\gas\unko2-kanri\コード.js / index.html
- デプロイ: clasp deploy -i {ID} -d "番号_内容" で上書き更新のみ

---

## SS・スクリプト・デプロイID一覧

| 役割 | SS ID | WebApp デプロイID |
|------|-------|-----------------|
| ①修正用SS | .clasp.json参照 | AKfycbw7rzkd_SuE1I6BNzEjED4Mxl6cnM4wbswIiRiNoPf5zcSS2JcP6YLkfRV21fLc0opU |
| ②客用SS | 1NBtosd_MN8KcboV_4OXTrY8WqcE3TJwpxdA_nASmTOo | AKfycbxP6x0cdhr8WzUaP-u_XlspPwY9EGvh8D3qleTVeOKRxawZFmV6rabbbR1ROHYzTvhD |
| ③各客SS | 会社登録シートF列 | 会社登録シートG列 |

---

## アーキテクチャ（3層構成）
①修正用SS（ライブラリ UnkouLib）→②客用SS（テスト）→③各客SS（本番）
- 各SSは独自WebApp URL（ssId不要・完全独立）
- ライブラリ固定バージョン現在260
- pushしても②③は自動で変わらない（手動反映必須）

---

## ファイル読み込みルール（コンテキスト上限回避・必須）

- コード.js は必ず150行以下に分割して読め
- 一度にファイル全体をReadするな（1Mコンテキストエラーの原因）
- 必要なセクションだけ特定してから読め
- 分割例: Read lines 1-150 → Read lines 151-300 → 順番に読む
- 「1M context required」エラーが出たら即停止してユーザーに報告しろ

## チャット引き継ぎルール（必須）

- 1チャットで扱うタスクは1個だけ
- コード修正が完了してdeployしたら、次のタスクは新チャットで始めろ
- 新チャット開始時は必ずこのCLAUDE.mdを読んでからタスク確認しろ
- 「API Error: Usage credits required for 1M context」が出る前に引き継ぎ文を書いてユーザーに渡せ
- コンテキストが重くなってきたと感じたら（Read回数が5回超えたら）即座に引き継ぎ文を出せ

### 引き継ぎ文のフォーマット
状況報告と次チャットへの指示をこの形式で出せ：

---次チャットへの引き継ぎ---
【完了したこと】
- （何をどう直したか1行で）

【次チャットでやること】
CLAUDE.mdを読め。タスク：（内容）。
関連コードは コード.js の（行数）行目あたり。
（注意事項があれば）
---ここまで---

---

## デプロイ手順

### ①修正用SSへのデプロイ
 = "AKfycbw7rzkd_SuE1I6BNzEjED4Mxl6cnM4wbswIiRiNoPf5zcSS2JcP6YLkfRV21fLc0opU"
clasp push --force
clasp deploy -i  -d "番号_内容"

### ②客用SSへのスタブpush
cd stub_for_clientSS
clasp push --force

---

## 永続ルール（絶対守れ・毎回確認）

### 作業ルール
1. 依頼してない箇所は絶対壊すな・消すな・変えるな
2. 実走前に必ずやる事を日本語で説明 → ユーザーOK後に実走
3. 全部確認してこっちがOK言ったら実走して
4. 不明点は聞け
5. 毎回このタスク一覧を出して状況確認してから作業開始
6. 毎回履歴（コード・git log）見て確認してから作業しろ
7. clasp push まで自分でやれ。ユーザーはF5押すだけ

### 説明ルール
- コードで話すな。依頼内容に沿って日本語で話せ
- 修正内容の説明禁止。操作手順（何を押すか）を毎回書け
- 要約・まとめ文は不要。結果と次の操作だけ書け

### 設計ルール
- ボタン遷移は1個ずつ（絶対遷移最優先）
- 処理はSSに送り裏で処理 → SSから値で返す（アプリを絶対重くするな）
- アプリはSSから値で受け取って表示するだけ

### SS紐づけ大原則
- companySsId を受け取る関数は必ず getTargetSS_(companySsId) で取得
- SpreadsheetApp.getActiveSpreadsheet() をAPIとして呼ばれる関数で使うな
- 反映ボタンはシート構成・書式のみ。データ行は絶対消さない

### 新規客SS作成ルール（絶対守れ）
- createCompanySpreadsheet_ は makeCopy 後に必ず initClientSSSheets_ を呼べ（②のデータ混入防止）
- 各シートの1行目（2行目）は必ずテストデータを書くこと
- 設定シートはデフォルト値のまま全行使う（変えるな）
- 自車専属マスタのテストデータは2行必須：
  - 1行目：給料テスト行（給料に値・%は空欄）
  - 2行目：%テスト行（給料は空欄・%に値）
  - 給料と%は同一行に両方書くな（片方だけ有効）

### スタブ同期ルール（絶対守れ・deploy のたびに必ず実行）
修正用SSでテスト → 客用SSでテスト → 各客に反映　の順でユーザーが手動確認する。自動で各客に反映するな。
ただし「反映ボタンを押したらいつでも修正用SSと同じになる」状態に必ず整えておけ。

1. `clasp push --force` + `clasp deploy` → ①修正用SS（ライブラリ）を更新
2. `cd stub_for_clientSS && clasp push --force` → ②客用SSのスタブを更新
3. `stub_for_clientSS/appsscript.json` の `"version"` を最新のデプロイ番号に更新してからpush
4. 新しいpublic関数を追加したら `getClientStubSource_()` と `stub_for_clientSS/コード.js` の両方に追加
5. ①修正用SSと②客用SS、どちらから「📤 全客SS（③）に反映」を押しても同じ結果になること

ユーザーの操作順序：
①修正用SSでF5テスト → ②客用SSでF5テスト → ②（または①）で「📤 全客SS（③）に反映」ボタンを押す

### デプロイルール
- デプロイ名は「番号_依頼内容（簡潔）」で命名
- 例: 270_行追加時刻9時固定バグ修正
- **デプロイのたびに必ずバージョン数を確認しろ（`clasp versions` で件数を見ろ）**
- **190個以上あったらデプロイ前にユーザーに報告して古いバージョンの削除を依頼しろ**
- バージョン削除の場所：①修正用SS → 拡張機能 → Apps Script → 左メニューの時計アイコン（バージョン管理）

---

## 最初にやること（新チャット開始時）

1. このCLAUDE.mdを最初から最後まで読む
2. 下記タスクリストの「今」の項目だけ抽出してユーザーに見せる
3. ユーザーにOKもらってから作業開始（コード全部読むのは必要になった時だけ）

---

## タスクリスト（旧）

| # | 内容 | 状態 |
|---|------|------|
| 1〜8 | 旧タスク全完了 | 済 |

---

## ロードマップ【M&Aエグジット 1億円】

| # | 内容 | 状態 | 目的 |
|---|------|------|------|
| 1 | システムの防弾化（排他ロック・認証修正） | ✅ 済 | 買収監査落ち・データ崩壊の阻止 |
| 2 | 【開発】客データ一括読込（配車表CSV/Excel） | **今** | 配車マンの手打ち面倒による初期解約を防ぐ |
| 3 | 【開発】自動送信機能（指示書・車番連絡など） | 未 | 現場の二度手間を消して定着させる |
| 4 | 【保守】引き継ぎ資料・マニュアルの完成 | 未 | 買い手の「社長が抜けたらメンテできない」不安を消す |
| 5 | 【法務】利用規約などのリスク封じ込め | 未 | 損害賠償などの致命的な法的リスクを塞ぐ |
| 6 | 【武器】PL作成・ETC明細読込などの追加機能 | 未 | 商談時の「これがあれば儲かる」という決定打にする |
| 7 | M&Aプラットフォーム掲載・交渉開始 | 未 | 1億円での売却完了 |

---

## 次チャット用テンプレート（コピペして使え）

### ロードマップ#2 客データ一括読込
CLAUDE.mdを読め。今やること：ロードマップ#2「客データ一括読込（配車表CSV/Excel）」。配車マンがExcel/CSVをアップロードして運行データを一括登録できる機能を作る。1個ずつ確認しながら進めろ。

