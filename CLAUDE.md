

Claude · MD
■ システム大原則（最最優先）
①修正用SSが唯一の絶対神であり、②客用SS（テンプレート）および③各客SS（顧客本番環境）は、その機能を「間借り」しているだけである。

修正用SSと異なる挙動、異なるメニュー、異なる独自コードを各客SSに直接書き込む行為は、いかなる理由があってもすべて重大なシステム破壊（バグ）とみなします。機能追加や修正は、必ず「①修正用SS」に対してのみ行い、所定の配信（デプロイ）プロセスを経て下流へ反映させてください。

3層配信（デプロイ）アーキテクチャの詳細構成・デプロイフロー・ID一覧は、本ファイル下部「■ 技術仕様・構成管理」を参照。

■ M&A譲渡・保守開発における鉄則（引き継ぎエンジニア向け）
本システムをM&Aにより譲り受けたエンジニア、または今後の保守・開発を担当するチームは、以下の設計思想と制御ルールを必ず遵守してください。これを無視した開発は、顧客環境のデータ全喪失や、起動トリガーの無限増殖（画面フリーズバグ）を引き起こします。

1. トリガー無限増殖・多重発火の徹底的自己防衛ロジック
Google Apps Script（GAS）のコンテナバインド型スクリプトは、SSを物理コピーした際に「見えない古いプロジェクトや起動トリガーの残骸（幽霊トリガー）」が裏側に蓄積し、起動時に同一のアラートが10回以上重複起動する仕様上の罠があります。 本システムでは、このバグを構造的に根絶するために以下の「自己防衛クリーンアップ・排他制御」を強制実装しています。

text
【自己防衛ライフサイクル（クリーンインストール）】
1. SS横断全トリガーの強制一括削除：
   各客SS（③）の反映・初期設定時、および起動時に、現在のアクティブプロジェクト内だけでなく、バインドされているすべてのスクリプトに跨るトリガーを「ScriptApp.getUserTriggers(ss)」で完全に走査し、一度すべて「物理的に完全削除（deleteTrigger）」する。
2. 自己消去型コードのデプロイ：
   古い残骸スクリプト（neutralizeScript_で処理されたプロジェクト）が万が一発火した場合は、自らの中に登録されているすべての古いトリガーを即座に「自己消去（自爆）」させて活動を完全に停止させる。
3. ドキュメントロックによる30秒デバウンス（二重起動ガード）：
   checkMasterExpiries等のダイアログ表示関数の先頭には、必ず「LockService.getDocumentLock()（スプレッドシート単位の共有ロック）」と「PropertiesService.getDocumentProperties()での実行タイムスタンプ管理（EXPIRY_POPUP_TS）」を噛ませ、30秒以内の複数回の多重起動を物理的に即 return させてブロックする。
※日常的なバグ修正やロジック変更は、①修正用（ライブラリ）を更新するだけで、下流の各客SS（③）は一切触らずに自動反映（差分反映）されます。各客SSのスタブやトリガーを直接触る必要があるのは、「起動時トリガーの構成自体を変更した時」または「新規SS作成時」のみです。

【最優先・絶対厳守】日常の開発・作業ルール
1. 作業前に必ず行うこと
新しいタスク（修正・機能追加）を始める前に、このマスターガイド（CLAUDE.md）を全文読み返す。
「引き継ぎメモ.md」が存在する場合は、その進捗と変更履歴を必ず100%理解してから着手する。
過去の指示や現在のコード構造に少しでも矛盾・不明点を感じた場合は、推測で進めず、作業を中断して必ずオーナーに質問すること。
2. 絶対遵守コーディングルール
秘密情報の完全非開示: APIキー、トークン、パスワード、接続文字列などの機密情報は、絶対にチャットやログに出力しない。
限定修正原則: 指定された箇所以外のコード、ファイル、シート構成は、絶対に触らない、壊さない、消さない、変えない。
ついで修正の禁止: 指示されていない箇所の「ついでにリファクタリング」「コードの整形」「不要に見える関数の削除」は、意図的なシステム構造を破壊するトリガーとなるため、絶対に実行しない。
1ファイル・1デバッグ: 複数ファイルにまたがる修正であっても、必ず1ファイルずつdiff（変更差分）を提示し、オーナーの「OK」を得てから適用・次のファイルに進む。
3. ドキュメント記述ルール（コード番号の紐付け）
コード内や報告書において、改修箇所や関連機能を明記する際は、必ずファイル名とコード番号を対にして記述すること。
記述例: コード.js: 3-2 / terms.html: 5番 / csvImport.html: 13-0〜13-8
4. セッション管理とトークン節約
セッション肥大化への対策: 会話ややり取りが長引いてきた場合は、AIから能動的に「ここまでの決定事項を引き継ぎメモ.mdにまとめ、新規セッションに移行しましょう」と1行で提案する。
確認・報告の極小化（トークン節約ルール）:
作業着手前の「本当に実行していいですか？」等の不要な確認ターンは禁止。指示が出たら即座にコードを解析し、diffを実行・提示すること。
ただし、diff提示とコードへの適用（Edit実行）は明確に別ターンとする。オーナーの明示的な「OK」が出るまで、いかなる場合もEdit・デプロイを実行しない。「diffを見せる＝確認を取った」ではない。これは確認ターンの省略対象に含まれない。
唯一の例外：CLAUDE.md・引き継ぎメモ.md等のドキュメントファイルへの誤字修正・コメント追記のみ、diff提示と同時適用を許可する。コード（.js/.html）は例外なくOK必須。
報告は「結論1行＋必要最小限の差分」のみ。愛想の良い締め文句（「〜しました。ご確認よろしくお願いします」等）はすべて省き、極限まで短縮する。
正常完了時の報告は**「完了」**の1行のみとする。問題やエラーが発生した場合のみ、詳細な原因と対策を報告する。
■ 技術仕様・構成管理
1. システム概要
実行プラットフォーム: Google Apps Script (GAS) V8ランタイム + SpreadsheetApp + PropertiesService
ローカル開発環境: c:\gas\unko2-kanri\
デプロイコマンド: node deploy.js "デプロイ時の修正説明"
2. 3層配信（デプロイ）アーキテクチャ
text
① 修正用SS（マスター / ライブラリ）
   │
   ├─ 「📤 客用SS（②）に反映」：最新コードをライブラリバージョンとして固定し、②に配信
   ▼
② 客用SS（配布用テンプレート / スタブ経由でライブラリ呼び出し）
   │
   ├─ 「📤 各客に反映」：契約中の全顧客環境（③）に対して、コードのクリーンインストールを実行
   ▼
③ 各客SS（G運輸、A運輸など、顧客ごとの完全独立シート）
3. SS・スクリプト・デプロイID一覧
役割	SS ID / 参照先	WebApp デプロイID
①修正用SS	.clasp.json を参照	AKfycbw7rzkd_SuE1I6BNzEjED4Mxl6cnM4wbswIiRiNoPf5zcSS2JcP6YLkfRV21fLc0opU
②客用SS	1NBtosd_MN8KcboV_4OXTrY8WqcE3TJwpxdA_nASmTOo	AKfycbxP6x0cdhr8WzUaP-u_XlspPwY9EGvh8D3qleTVeOKRxawZFmV6rabbbR1ROHYzTvhD
③各客SS	会社登録シートF列を参照	会社登録シートG列を参照
4. 物理ファイル構成
text
コード.js                ← メインライブラリ（すべての業務ロジック・コア処理をここに実装）
index.html               ← モバイルWEBアプリ（フロントエンドUI）
stub_for_clientSS/
  コード.js               ← ②および③の環境に配置されるスタブ（ライブラリ呼び出し専用）
  appsscript.json        ← deploy.js が自動更新するプロジェクト設定ファイル
build_stub.js            ← getClientStubSource_() 自動生成ユーティリティ（Node.js）
check_integrity.js       ← デプロイ前整合性チェック（HTML内の未定義スタブ関数検知用 / Node.js）
deploy.js                ← 整合性チェックからclasp push、バージョン管理まで行う自動デプロイスクリプト
5. 開発規約 & 制約事項
PropertiesServiceによるロック: 同一レコードや同時処理の衝突を防ぐため、データ処理には必ず PropertiesService による排他制御（ロック）をかける。
SpreadsheetAppの参照規則: フロントエンド（google.script.run）経由で実行される関数内では、絶対に SpreadsheetApp.getActiveSpreadsheet() を使用してはならない。必ず引数から ssId を受け取るか、トリガーイベント e.source を利用してスプレッドシートを特定すること。
トリガー登録の責務: 各種トリガー（編集検知、時間起動等）の登録ロジックは、ライブラリ側ではなく、顧客環境に物理配置される「スタブ（stub_for_clientSS/コード.js）」に実装すること。また、登録時は前述の「全削除 → 新規登録」フローを徹底すること。

