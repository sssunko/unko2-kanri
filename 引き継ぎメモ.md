# 引き継ぎメモ（2026-07-16 更新）

## 直近のデプロイ履歴

| バージョン | 内容 |
|-----------|------|
| v765 | 完全ノータッチ原則対応: 自車専属マスタB列変更→syncVehicleToCurrentMonth_をsimpleトリガー直呼び（初期設定不要化）・A1メモ色付き丸・check_integrity#6プライベート関数禁止チェック追加 |
| v763 | A1凡例メモ記号〇統一（白丸→絵文字修正途中） |
| v759 | スタブonOpenのプライベート関数呼び出し3件修正（applyHolidayRowColors_/applyExpiryWarningColors_/convertLegacyAdminDataUrls_→公開ラッパー追加） |
| v758 | cleanupStaleTriggers の getUserTriggers 修正・check_integrity に getProjectTriggers 禁止チェック追加 |
| v756 | A運送③客SS削除→再作成・syncToAllClientSS実行（ゾンビトリガー物理根絶） |
| v751 | デコイ作戦: checkMasterExpiries を空デコイ化・実処理を showExpiryAlert() に移行 |
| v697 | 反映時に①の列数を超えた余分な列を強制削除 |
| v699 | 全シート調査→5箇所の不整合修正（自車専属マスタ37列化・集計表ヘッダー・dispatchStructureChange allowed・CSVインポート辞書ガード条件） |
| v701 | 反映時にCSVインポート辞書を自動初期化（反映+F5だけで全項目揃う） |
| v702 | 自車専属マスタ重複列自動削除・コンプライアンス4列の日付書式・AF〜AM黒背景緑文字・トリガー重複除去・ID列期限色の自動更新 |
| v703 | 日付フォーマットをyyyy年M月d日 → yyyy/M/d に変更（setNumberFormat3箇所） |

---

## 現在進行中のタスク

### 運行シート機能一覧の整理（説明書・バグチェック用）
ユーザーが提供したリストを分類・並び替え中。以下が現時点の確定版。

| 番号 | カテゴリ | 名前 | 色 | 説明 | コード番号 |
|------|---------|------|-----|------|----------|
| 1 | 自動入力 | ID自動採番 | なし | B～列のどこかに入力するとIDが自動で入る | コード.js: 3-2 |
| 2 | 自動入力 | トン数正規化 | なし | D列（トン数）に入力すると正規化される | コード.js: 3-2 |
| 3 | 自動入力 | 車番自動補完 | なし | F列（車番）に入力するとマスタから乗務員名等が補完される | コード.js: 3-2 |
| 4 | 自動入力 | 日付に現在時刻付与 | なし | J列（日付）に日付のみ入力すると時刻が自動付与される | コード.js: 3-2 |
| 5 | 自動入力 | 車番の正規化・日付合成 | なし | N～R列（点呼前後完了）入力時に正規化・日付合成 | コード.js: 3-2 |
| 6 | 自動入力 | 金額・時刻の書式自動整形 | なし | 編集後にCS～列にコンマ付き書式が自動設定 | コード.js: 4-2b |
| 7 | 自動計算 | 積込高速〜代実高速代への自動コピー | オレンジ文字 | T列（積込高速代）を入力すると実高速代に自動コピー | コード.js: 3-2 |
| 8 | 自動計算 | 経費自動計算セット | なし | V列（経費）の数式が自動設定される | コード.js: 3-2, 1-10 |
| 9 | 自動計算 | 日付変更時の自動ソート | なし | J列（日付）を変更すると運行シートが自動ソート | コード.js: 4-1, 4-2 |
| 10 | 自動計算 | 積地・降地→距離自動計算 | なし | L列（積地）またはM列（降地）に入力すると距離が計算される | コード.js: 14-2b |
| 11 | 自動計算 | 集計表の自動補完 | なし | 運行シートを編集すると集計表に自動反映 | コード.js: 4-3, 4-4, 4-5 |
| 12 | バリデーション | 時刻入力間隔バリデーション | なし | 帰着→積完→休憩開始→休憩終了の順序をチェック | コード.js: 3-2 |
| 13 | バリデーション | マスタ連携ID警告 | なし | 自車専属マスタにIDが見つからない時に警告 | コード.js: 3-2 |
| 14 | バリデーション | ID車複元警告 | 赤（A列のみ） | 同IDで車番や日付が食い違う行を赤で警告 | コード.js: 9-2, 8-6c |
| 15 | 色・表示 | 有休行の色付け | 薄グレー | 積地列に「有休」と記入すると行全体が薄グレーになる | コード.js: 3-2, 1-7 |
| 16 | 色・表示 | 休み行の色付け | 濃グレー | 積地列に「休み」と記入すると行全体が濃グレーになる | コード.js: 3-2, 1-7 |
| 17 | 色・表示 | 業務員資格期限アラート（A列） | 薄赤 | SS開くたびに自車専属マスタを参照して期限切れのA列を薄赤にする | コード.js: 1-7c |
| 18 | 色・表示 | A1セルへの色凡例のメモ | なし | A1セルにセルの色の意味が表示される | コード.js: 3-2 |
| 19 | 色・表示 | ID車複元 | なし | 1行目が変更されると元に戻す | コード.js: 1-7b |
| 20 | 手動操作 | ID・車番→請求補完（手動） | なし | メニュー→「ID・車番」一覧から手動で請求書に補完 | コード.js: 4-2b |
| 21 | 手動操作 | 手動日付強制並び替え | なし | メニュー→「日付順並び替え設定」で手動ソート | コード.js: 1-10 |
| 22 | 送信・帳票 | 集計・請求データへのファイル添付 | なし | 集計・請求データにファイルを添付する | コード.js: 14-2c, 14-2e, 14-4, 14-5, 14-6〜14-10 |
| 23 | 送信・帳票 | 発注書・指示書送信 | なし | メニュー→「発注書送信」で指示書をメール送信 | コード.js: 14-2d, 14-2e, 14-4, 14-5 |
| 24 | 送信・帳票 | 車番連絡送信 | なし | メニュー→「車番連絡送信」でメール送信 | コード.js: 1-7c |
| 25 | 外部取込 | 配車表CSV/Excel取込 | なし | メニュー→「CSVをExcelに」で配車データを取込 | csvImport.html: 13-0〜13-8 |
| 26 | 外部取込 | 配車板からの配車確定 | なし | 配車板でチェックした行が運行シートに反映される | コード.js: 15-1, 2-1, 3-5a |

---

## 未解決・未確認事項

- v703の日付フォーマット変更（yyyy/M/d）は反映ボタンを押さないと②③に適用されない。ユーザーがまだ反映していない可能性あり。
- 運行シート機能一覧はユーザーが確認中。確定したら説明書作成に進む予定。

---

## 絶対に守るルール（次セッションでも継続）

- diff見せて止まる・OKもらってから適用
- 指定外のコード・ファイルは一切触らない
- コード番号はファイル名：番号の形式（例: コード.js: 3-2）
- デプロイ後の案内は「修正用SS → F5 → 反映ボタン」の1行のみ
- 実装前の説明はコードなし・日本語・動作ベース
