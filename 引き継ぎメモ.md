# 引き継ぎメモ（③小計画専用）

## この計画構造について

このプロジェクトは①CLAUDE.md（恒久ルール）→②運行シート書リスト.md（進行中の大タスク進捗）→③本ファイル（今実走中の小タスクのみ）の3層で管理されている。
新セッション開始時は必ず①→②→③の順に読むこと。本ファイルには③の情報しか書かない。

## 現在バージョン

v1154（前回セッション末）

## 今実行中の小タスク

集計表 1番（運行シートからの自動同期）

## 必読ファイル

- 集計表.md（バグチェックリスト全18項目＋書リスト）
- 運行シート.md（完了済み参照用）
- HP: https://smart-simple-system.smart-simple-system.workers.dev/manual-user（大→中→小ドリルダウン）

## SSシート更新手順（バグチェック番号完了ごとに実施）

バグチェック番号が1つ完了したら以下を実行：

1. コードを読んでバグ有無・仕様を把握し、テスト手順を1〜2行でユーザーに指示する
2. ユーザーが①修正用SSで動作確認してスクショをチャットに貼る
3. Claude Codeがスクショを自動保存（ユーザーに頼まない）
   → Write toolでスクラッチパッドに書き出し → Bash mvで manual-deploy/deploy-assets/screenshots/XX-機能名-連番.png に保存
4. manual-deploy/deploy-assets/manual-user.html と manual-ma.html の集計表03カードに内容・スクショを追記
5. ユーザーにOK確認（1行のみ）
6. OK後：集計表.mdの「確認済み機能 詳細説明」を更新 → `node deploy.js "集計表 XX番追加"` → wrangler deploy（manual-deploy/ から）

## 次にやること

集計表 1番（運行シートからの自動同期）のバグチェック→スクショ取得→HTML更新
