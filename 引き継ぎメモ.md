# 引き継ぎメモ（2026-08-04 更新）

現在：v868　**バグチェック中。全41項目。済：1〜14・16〜18・20番（18項目）。次：15番（ヘッダー行の自動復元）**

**必ず読むこと：**
- `project_bugcheck_list.md`（進捗・未項目）
- `project_bugcheck_done.md`（済詳細・HP化手順・開発メモ）
- `feedback_reflect_architecture.md`（反映の仕組み）

**バグチェックの進め方：**
1. `project_bugcheck_list.md` の次番号を確認する
2. 該当番号のコード番号を確認し、`コード.js` を grep で該当行を特定して部分 Read する
3. 確認内容（①②③）を実機で検証する（実走前に「何をするか説明 → ユーザーOK → 実走」）
4. バグがあれば修正して `node deploy.js "説明"` でデプロイ
5. ユーザーが「OK」と言ったら：①番号を済に変更 ②説明文書いてチャット出す ③`project_bugcheck_list.md` 更新
6. HP化（スクショ貰ったら）：jsonlから画像抽出→screenshots/保存→html埋め込み→deploy（詳細は `project_bugcheck_done.md` の「HP化の定義」参照）
7. 次の番号へ

**データ汚染：** 解決済み（2026-08-04）。コードへの影響なし確認済み。

## デプロイ履歴
| バージョン | 内容 |
|-----------|------|
| v845 | 全変更revert・3455ac5の状態に完全復元 |
