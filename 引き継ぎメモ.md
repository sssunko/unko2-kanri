# 引き継ぎメモ（2026-07-29 更新）

## 現在のバージョン：v845（コードはgitコミット 3455ac5 の状態に完全復元済み・デプロイ済み）

---

## 最重要・次セッションで最初にやること

### ステップ0：着手前に必ず読め
1. このファイル全文
2. メモリ `project_bugcheck_list.md`（バグチェック進捗）
3. メモリ `feedback_reflect_architecture.md`（反映の仕組み）
4. 下記「■ 今回セッションで起きた事故と教訓」を必ず読んでから着手する。同じ失敗を繰り返すと信用を失う。

### ステップ1：①修正用SSの実データ汚染を確認せよ（コードより先）
今回のセッション中、①修正用SSの「自車専属マスタ」シートに対して列の追加・移動・削除を何度も実行した。**コードはgit revertで元に戻したが、シートに実際に書き込まれた結果（列構成・データ）は戻っていない。**
現状確認されている汚染：
- AL列付近に「PL設定按分（参照）」という列が残っており、中身が「2646/06/08」のような明らかにおかしい値（日付と金額が混ざった壊れたデータ）になっている
- 免許証有効期限列に本来入っているべきでない値が入っている可能性がある
- 担当管理者列のデータ（「片岡」等）が一部の行で消えている可能性がある

**次にやること：** ユーザーと一緒に①修正用SSの自車専属マスタを直接目視確認し、どの列・どの行が汚れているかを特定してから、汚染除去とバグ修正を計画する。コードだけ直しても実データは直らない。

### ステップ2：バグチェックリスト13番（資格期限アラート色・A列）
コード自体はv845時点（＝今回のセッション開始前の状態）で以下の仕様になっている。コードは今回一切変更していないので、この時点の実装が正しいかどうかを実機で確認する。

確認内容：
- 自車専属マスタに期限が登録されている乗務員がいる行のA列が、SS開き直し（F5）後に色が付くか
  - 期限超過 → 淡い赤（#ffcdd2）
  - 当日 → 淡い青（#bbdefb）
  - 7日以内 → 淡い緑（#c8e6c9）
- 有休・休み行（グレー行）には期限色が上書きされないか
- ID衝突行（濃い赤 #ff1744）には上書きされないか

対象コード：`applyExpiryWarningColors_`（コード.js、v845時点のコード。今回セッションで追加した「免許証有効期限セル自体を赤/黒にする」ロジックは全部revertで消えているので、その機能は今は存在しない）

### ステップ3：ユーザーが指摘している4つの課題（着手はステップ1完了後）
以下はコードにまだ一切手を付けていない。次セッションで①から順に、1つずつ説明→OK→実走で進めること。

**①免許証有効期限が金額になる**
自車専属マスタの「免許証有効期限」列に、日付ではなく金額のような数値が入ってしまう現象。恐らく「経費自動入力」を実行したときに、この列の位置に本来別の用途（PL設定按分の参照列）のデータが書き込まれてしまっている。位置がズレて衝突している可能性が高い。

**②経費自動入力の色が期限にも反映されている**
経費自動入力を押すと自動入力を示す色（緑文字）が免許証有効期限にもついてしまう。免許証有効期限は経費と無関係なので、基本は黒文字であるべき。

**③免許証有効期限セルの色分け**
免許証有効期限そのものが期限切れなら赤文字、それ以外は黒文字になるようにする（現状そういう仕組みが無い）。

**④ヘッダー色をカテゴリ別に分離**
自車専属マスタのヘッダー行で、経費列・期限列（免許証有効期限〜適性診断次回予定日）・担当管理者列の色が今は一色に混ざっている。経費は経費、期限は期限、担当は担当で別の色に分ける。

---

## ■ 今回セッションで起きた事故と教訓（絶対に読んで理解してから着手すること）

今回のセッションで①②③④に対して複数回パッチを当てたが、以下の理由で状況を悪化させた。同じ過ちを繰り返すな。

1. **列位置を決め打ちで計算するコードが複数箇所に散らばっていた。** 見出し名で毎回検索する方式に直したつもりでも、見落としがあり後から別の箇所（`refreshPlApportionColumn_`など）で同じ種類のバグが見つかった。**列を触るコードを書く前に、必ずコード全体を grep して同じパターンの他の箇所がないか確認してから着手すること。**

2. **`.clearContent()` は値しか消さない。文字色などの書式は残る。** 誤って書き込まれた値を消すときは `.clearContent().clearFormat()` を使うこと。忘れると「消したはずなのに色だけ残る」バグになる。

3. **重複列を「後ろのものを問答無用で削除する」ロジックは危険。** データが入っている方の列が後ろに来るケースがあり、その場合は本物のデータごと削除してしまう（実際に担当管理者のデータが消えた）。重複列を消すときは、**必ずどちらの列にデータが入っているかを確認してから、データが無い方を消す**こと。

4. **PropertiesServiceの「一度きりフラグ」は壊れやすい。** セッション中に何度もバージョンを重ねて試行錯誤すると、未完成なロジックの状態でフラグが消費されてしまい、完成版のロジックが二度と実行されなくなる。列構成の自動修復のような重要な処理には、フラグより「毎回チェックして違えば直す」方式（ズレていなければ即終了なので負荷は無視できる）の方が安全。

5. **①修正用SSの実データに対して、検証未了のコードを何度も実走させた。** その結果、コードをgit revertしても実データの汚染だけが残った。**列を追加・移動・削除するようなコード（`insertColumnBefore`・`deleteColumn`）は、実行するたびに実データへ不可逆の影響を与える。** 少なくとも1回ごとにユーザーと一緒に結果を確認し、想定通りでなければ即座に立ち止まること。「直したつもり」で次々に上書きパッチを当てるのは絶対にやってはいけない。

6. **同じ処理（ヘッダー色付けなど）が3箇所にコピペされていた。** 1箇所直しても他の2箇所は直っておらず、後で気づいて慌てて修正することになった。同種の処理を見つけたら、修正前に「他に同じコードが無いか」を必ず確認する。

---

## 絶対守るルール（毎回・例外なし）

1. **実走前に何をするか説明 → ユーザーOK → 「コード壊さない・消さない・変えないまま実走します」と言ってから実走**
2. 指定箇所以外のコード・ファイルは一切触らない・変えない・消さない
3. 列の追加・移動・削除（insertColumnBefore・deleteColumn）を含む修正は、1回実走するごとに必ず結果を確認してから次に進む。連続で何本もパッチを重ねない
4. 説明はコードなし・日本語・動作ベースで（実装前も原因報告も同様）
5. 実装前の説明の冒頭で必ず「今後コード使わず簡潔に説明します」と宣言してから話す
6. OKと言われたら1セット：①番号を済に変更②説明文を書く③チャットに出す④リストを更新
7. デプロイ完了の報告：「デプロイ完了　v[番号]　[内容]」＋反映方法を短く

---

## 直近のデプロイ履歴

| バージョン | 内容 |
|-----------|------|
| v845 | 今回セッションの変更を全てrevert。直前コミット（3455ac5）の状態に完全復元 |
| v838〜v844 | PL設定按分列・免許証有効期限まわりの一連の修正（全てv845でrevert済み。中身は上記「教訓」参照） |

コミット履歴上の直近：`3455ac5 7281812　12まで`（このコミットの状態＝v845の中身と一致）
