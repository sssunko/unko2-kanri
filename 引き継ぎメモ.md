# 引き継ぎメモ（2026-07-24 更新）

## 現在のバージョン：v823

---

## 最重要・次セッションで最初にやること

### 4番のバグを修正してから5番に進む

**バグの内容：**
E列（車種）に大文字や全角英字で入力されたとき小文字に変換される機能がある。
しかし、F列（車番）を入力してマスタから自動補完されたとき（マスタのE列データが大文字の場合）は変換が走らない。

**原因（コード.js line 1637）：**
E列変換ブロックの条件が「E列を直接編集したとき」に限定されているため、F列補完経由でE列に書き込まれた値は変換されない。

**修正方法（絶対守れ）：**
- F列補完処理（コード.js line 1709付近の `sheet.getRange(row, 2, 1, 8).setValues(...)` ）は**一切触らない**
- F列補完のforループ（line 1665〜）が全行終わった後に、E列（車種）を一括スキャンして小文字変換するブロックを新たに追加する
- これだけ。他は何も変えない。

**実走前ルール（絶対）：**
修正内容を日本語で説明 → ユーザーが「OK」と言ったら実走。これを破ったら終わり。

---

## バグチェックリストの進捗

メモリの `project_bugcheck_list.md` に全28項目のリストがある。必ずそこを読め。

**現在の状態：**
- 1〜4番：済（ただし4番に上記バグが残っている → 修正後に改めて済とする）
- 5番以降：未

**4番の正しい説明文（修正完了後にリストに入れる）：**

【入力時・自動】
・E列（車種）に大文字や全角英字（「Ｗ翼」「TRUCK」など）で入力しても自動で小文字・半角に変換して保存される
・F列（車番）を入力してマスタから自動補完されたときも、マスタ側の車種データが大文字であれば小文字に変換される

【変換対象】
・全角アルファベット（Ａ〜Ｚ、ａ〜ｚ）→ 半角に変換
・大文字 → 小文字に変換

**5番の確認事項（4番の修正後に着手）：**
メモリの `project_bugcheck_list.md` の5番を参照。

---

## 絶対守るルール（毎回・例外なし）

1. **実走前に何をするか説明 → ユーザーOK → 実走**（これを破ったら即終わり）
2. 指定箇所以外のコード・ファイルは一切触らない・変えない・消さない
3. F列補完処理（コード.js: line 1709付近）は絶対に触らない
4. 説明はコードなし・日本語・動作ベースで
5. 修正前に「何をするか」→OK→「コード壊さない消さない変えないまま実走します」→実走
6. デプロイ完了の報告：「デプロイ完了　v[番号]　[内容]」＋「修正用SS → F5 → 📤 テスト客SS（②）に反映」のみ

---

## 直近のデプロイ履歴

| バージョン | 内容 |
|-----------|------|
| v823 | v822リバート：F列補完でのE列小文字化を元に戻す |
| v822 | E列小文字変換をF列補完時にも実行（→ユーザー指示でv823でリバート） |
| v820 | 空白行をソート末尾固定＋物理削除・点呼順序チェック修正 |
| v819 | 点呼前→誘導順序チェック追加 |
| v817 | 全角数字の時刻変換追加（N〜R列・点呼前後列） |
| v814 | ソート後の背景色・リッチテキストリンクズレ修正 |
