# 運行管理システム 開発会話履歴サマリー
作成日: 2026-05-15

## システム概要
- Google Apps Script (GAS) V8ランタイム
- マスターSS（コード・全機能） + 各会社SS（表のみ）
- WebアプリURL: `https://script.google.com/macros/s/AKfycbw5MLHFep_jOQEdAg4_wX8LMGPX7wVL41XbmygqVV794LkZu6Xv-XcRLNAHYqg9bd0fyw/exec`
- デプロイID同上（@106まで進行）

---

## 確定アーキテクチャ

| URL | 参照SS | 用途 |
|---|---|---|
| `https://...exec` (ssIdなし) | マスターSS | 管理者（admin）専用 |
| `https://...exec?ssId=A運輸のSSID` | A運輸SS | A運輸の乗務員専用 |
| `https://...exec?ssId=B運輸のSSID` | B運輸SS | B運輸の乗務員専用 |

- 会社SS = 表のみ（GASスクリプトなし）
- 各会社のアプリURLは別URL（ssIdが異なる）
- 情報漏洩なし・完全分離

---

## ファイル構成
- `c:\gas\unko2-kanri\コード.js` — メインGASコード
- `c:\gas\unko2-kanri\index.html` — フロントエンド
- `c:\gas\unko2-kanri\appsscript.json` — スコープ設定
- `c:\gas\unko2-kanri\deploy.ps1` — デプロイスクリプト

---

## appsscript.json（確定版）
```json
{
  "timeZone": "Asia/Tokyo",
  "webapp": { "executeAs": "USER_DEPLOYING", "access": "ANYONE_ANONYMOUS" },
  "oauthScopes": [
    "https://www.googleapis.com/auth/spreadsheets",
    "https://www.googleapis.com/auth/drive",
    "https://www.googleapis.com/auth/script.external_request",
    "https://www.googleapis.com/auth/script.scriptapp",
    "https://www.googleapis.com/auth/gmail.send"
  ],
  "runtimeVersion": "V8"
}
```

---

## 解決済み問題

1. `ScriptApp.getProjectTriggers` 権限エラー → `script.scriptapp` スコープ追加
2. 古いデプロイURL → `installTriggers()` に常時URLプロンプト追加
3. メニュー逆問題 → `__COMPANY_SS__` 隠しシートで判定（マスターSSにはない）
4. clasp push だけでは不完全 → 常に `deploy.ps1` 経由に統一
5. 会社SS作成 → `getOrCreateCompanyTemplate_()` + `makeCopy()` 方式
6. 同一Gmail複数会社問題 → localStorage + `?ssId=` 方式で解決

---

## 主要関数・設計メモ

### onOpen()
- `__COMPANY_SS__` シート存在 → 客用メニュー表示
- なし → 管理者メニュー表示（初期設定不要で自動判定）

### doGet(e)
```javascript
function doGet(e) {
  var ssId = (e && e.parameter && e.parameter.ssId) ? e.parameter.ssId : '';
  // webAppUrl を Script Properties に自動保存
  var tmpl = HtmlService.createTemplateFromFile('index');
  tmpl.companySsId = ssId;
  return tmpl.evaluate().setTitle('運行管理システム');
}
```

### getTargetSS_(ssId)（修正後）
```javascript
function getTargetSS_(ssId) {
  if (ssId) {
    try { return SpreadsheetApp.openById(ssId); } catch(e) {}
  }
  return SpreadsheetApp.getActiveSpreadsheet(); // マスターSS
}
```
※ linkedSsId UserProperties フォールバックを削除済み

### localStorage キー
```javascript
var LS_EMAIL_KEY = 'unkouLinkedEmail' + (COMPANY_SS_ID ? '_' + COMPANY_SS_ID : '');
```

---

## ssId対応状況（@106時点）

| 状態 | 関数 |
|---|---|
| ✅ 対応済み | `getInitialData(hintEmail, ssId)` / `linkAddress(email, ssId)` / `unlinkAddress(ssId)` |
| ❌ 未対応 | `getTodayRoutes` / `createParentRows` / `setGuideComplete` / `setPickComplete` / `setRest` / `setDropComplete` / `updateRouteData` / `deleteRunRows` / `clearTimeCell` / `getListData` / `getEditData` / `saveEditData` / `appendTerminalFile` / `deleteTerminalFile` / `replaceTerminalFile` / `getMyNotices` / `getRoutesById` / `getNoticeByRow` / `saveNotice` / `saveTerminalNotice` / `deleteRunById` など約20関数 |

→ @107以降で全関数ssId対応予定

---

## セットアップ手順（ユーザー操作）

1. マスターSSのメニュー「🔧 初期設定」
   → `https://script.google.com/macros/s/AKfycbw5MLHFep.../exec` を入力
2. 「会社登録」シートにA列（会社名）＋B列（Gmail）を入力
   → `installedOnEdit_` が自動起動 → SS作成 + メール送信
3. 会社担当者が受信メールの `?ssId=xxx` 付きURLを使用

---

## deploy.ps1
```powershell
$env:PATH = "$env:APPDATA\npm;" + $env:PATH
Set-Location $PSScriptRoot
clasp push --force
clasp deploy -i "AKfycbw5MLHFep_jOQEdAg4_wX8LMGPX7wVL41XbmygqVV794LkZu6Xv-XcRLNAHYqg9bd0fyw" -d "update"
```

---

## 注意事項
- "元のコード絶対壊さんといて"
- "新規コードに番号と説明書くこと"
- deploy は必ず deploy.ps1 経由（clasp push だけは不完全）
- 実走は OK が出るまでしない
